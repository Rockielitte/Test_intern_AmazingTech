import { Flex } from "antd";
import React from "react";

type Props = {};

const DashBoard = (props: Props) => {
  return (
    <Flex
      justify="center"
      align="center"
      style={{
        height: "100%",
      }}
    >
      Under development, move to other tabs
    </Flex>
  );
};

export default DashBoard;
